parameters:
    kernel.secret: ''

framework:
    test: true

services:
    _defaults:
        public: true

    subscriber:
        alias: rest.subscriber

    json:
        alias: rest.decoder.json

    xml:
        alias: rest.decoder.xml
